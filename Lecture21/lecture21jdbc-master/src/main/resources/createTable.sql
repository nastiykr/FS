CREATE TABLE customers (
                             age INTEGER NOT NULL,
                             first_name TEXT,
                             second_name TEXT not null
);

INSERT INTO customers VALUES (18, 'Peter', 'Parker');